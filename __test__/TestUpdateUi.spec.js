import { handleUI } from '../src/client/js/updateUI';
 
describe("Testing the submit functionality", () => {
    test("Testing the handleUI() function", () => {
           expect(handleUI).toBeDefined();
})});